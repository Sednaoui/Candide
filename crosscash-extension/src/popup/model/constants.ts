// TODO: move to server proxy when ready
export const CURRENT_NETWORK = 'ropsten';
export const INFURA_ID = '';
export const ALCHEMY_API_KEY = '';
export const ETH_PROVIDER = `https://${CURRENT_NETWORK}.infura.io/v3/${INFURA_ID}`;
