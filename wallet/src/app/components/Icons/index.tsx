import {
    faExternalLink,
    faCertificate,
} from '@fortawesome/free-solid-svg-icons';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';

export const externalLinkIcon = <FontAwesomeIcon icon={faExternalLink} />;
export const verifiedBadgeIcon = <FontAwesomeIcon icon={faCertificate} />;
